const doctors = {
    available = ""
}

import {getDoctorsOlivos} from './losOlivos'
import {getDoctorsSanPedro} from './sanPedro'


class University {
    constructor(specialty, email, name) {
        this.specialty = specialty,
        this.email = email,
        this, name = name
    }
}